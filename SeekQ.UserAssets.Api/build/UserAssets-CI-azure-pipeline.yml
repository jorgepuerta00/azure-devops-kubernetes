name : UserAssetsApi-CI
trigger:
  branches:
    include:
      - dev
  paths:
    include:
      - SeekQ.UserAssets.Api/*

pool:
  vmImage: 'ubuntu-latest'

variables:
  ImageName: 'jorgepuerta00/userassetsapi:$(Build.BuildId)'

stages:
- stage: Build
  displayName: Build image
  jobs:  
  - job: Build
    displayName: Build and push Docker image
    steps:
    - task: Docker@1      
      inputs:
        containerregistrytype: 'Container Registry'
        dockerRegistryEndpoint: 'Docker Hub'
        command: 'Build an image'
        dockerFile: '**/SeekQ.UserAssets.Api/Dockerfile'
        arguments: '--build-arg BuildId=$(Build.BuildId)'
        imageName: '$(ImageName)'
        useDefaultContext: false
        buildContext: 'SeekQ.UserAssets.Api'
      displayName: 'Build the Docker image'

    - task: Docker@1      
      inputs:
        containerregistrytype: 'Container Registry'
        dockerRegistryEndpoint: 'Docker Hub'
        command: 'Push an image'
        imageName: '$(ImageName)'
      condition: and(succeeded(), ne(variables['Build.Reason'], 'PullRequest'))
      displayName: 'Push the Docker image to Dockerhub'